uptime:
> clone the project and run the command *```./uptime.sh```* to run the script

evn_var:
> clone the project and run the command *```./env_var.sh```* to run the script

info:
> clone the project and run the command *```./info.sh```* to run the script

random password:
> clone the project and run the command *```./random_password.sh```* to run the script
